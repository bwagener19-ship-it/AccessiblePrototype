Testing Requirements:
- Ensure all core components of the application pass unit tests.
- Test must follow organisational guidelines:
  * Tests must be automated.
  * Tests must run inside the IDE.
  * All code paths must be tested (where possible).
  * All algorithms must have at least 1 input → output verification.
  * All errors/exceptions should be handled.
- Test framework required: unittest
- Test coverage target: Minimum 80%
- Status reporting required: Test Progress Report